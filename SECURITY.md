# Security policy

Report vulnerabilities privately to the repository owner. Do not include credentials, provider keys, or production data in issues, events, logs, test fixtures, or pull requests.

Treat all retrieved content and model output as untrusted data. Tool and repository write adapters must require an explicit, scoped capability grant with expiry and audit correlation.
